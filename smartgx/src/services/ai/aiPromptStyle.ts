/**
 * Shared style rules appended to SmartGX AI prompts (Gemini via proxy).
 * Keeps tone and currency consistent across features.
 */
export const SMARTGX_AI_WRITING_RULES =
  "Write in natural sentences. Do not use semicolons inside sentences. Use Malaysian Ringgit format such as RM100 or RM1,200. Do not use $, USD, or the word dollars. Base every claim on the JSON context. If something is missing in context, say so briefly instead of inventing numbers.";
